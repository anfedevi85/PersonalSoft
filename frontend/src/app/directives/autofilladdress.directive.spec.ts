import { AutofilladdressDirective } from './autofilladdress.directive';

describe('AutofilladdressDirective', () => {
  it('should create an instance', () => {
    const directive = new AutofilladdressDirective();
    expect(directive).toBeTruthy();
  });
});

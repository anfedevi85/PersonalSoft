import { SsnformatDirective } from './ssnformat.directive';

describe('SsnformatDirective', () => {
  it('should create an instance', () => {
    const directive = new SsnformatDirective();
    expect(directive).toBeTruthy();
  });
});

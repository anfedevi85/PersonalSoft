import { FormdateDirective } from './formdate.directive';

describe('FormdateDirective', () => {
  it('should create an instance', () => {
    const directive = new FormdateDirective();
    expect(directive).toBeTruthy();
  });
});

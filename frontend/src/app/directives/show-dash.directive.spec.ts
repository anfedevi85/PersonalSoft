import { ShowDashDirective } from './show-dash.directive';

describe('ShowDashDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDashDirective();
    expect(directive).toBeTruthy();
  });
});

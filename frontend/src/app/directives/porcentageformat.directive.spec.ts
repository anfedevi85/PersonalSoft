import { PorcentageformatDirective } from './porcentageformat.directive';

describe('PorcentageformatDirective', () => {
  it('should create an instance', () => {
    const directive = new PorcentageformatDirective();
    expect(directive).toBeTruthy();
  });
});
